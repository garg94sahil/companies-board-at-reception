# Copy this file to .env and fill in the real value. .env is gitignored — never commit it.

# AbleSign API key (AbleSign CMS > Account > API Keys). Used by
# tools/video/upload-to-ablesign.mjs to push a rendered center video live.
ABLESIGN_API_KEY=
